## Change request template (copy/paste)

### What you want to change

- **Goal**:
- **Why**:
- **User-visible behavior** (before → after):

### Where it shows up

- **Frontend pages** (URLs): (e.g. `/forums/:id`)
- **Frontend files** (if known):
- **Backend endpoints** (if known): (e.g. `PUT /api/forums/:id`)

### Data / API contract (if relevant)

- **Request body** (example JSON):
- **Response body** (example JSON):
- **Auth rules**:
  - [ ] Logged-out users
  - [ ] Logged-in users
  - [ ] Forum owner
  - [ ] Admin

### Edge cases / constraints

- **Validation rules**:
- **Performance constraints** (e.g. avoid loading all threads):
- **Deployment/env needs** (new env vars?):

### Acceptance criteria

- [ ] What must work after the change
- [ ] What must not break

